# Runtime dependencies for Vercel's Python function bundle.
# Mirrors [project].dependencies in pyproject.toml (base runtime set only -
# no dev/web/postgres extras, to keep the deployed bundle small).
langgraph>=1.2,<2
langchain-core>=1.0,<2
openai>=2.0
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
pyyaml>=6.0
pypdf>=5.0
beautifulsoup4>=4.12
numpy>=1.26
httpx>=0.27
structlog>=24.1
tenacity>=9.0
